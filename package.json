{
  "name": "meme-enhancer-9000",
  "version": "0.0.1",
  "description": "Meme Enhancer 9000 – Private browser editor. Upload Image/Video/GIF, add text/image layers, move them, and save as Image or GIF.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 9000",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 9000",
    "start": "node server.js",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "neutralino:update": "neu update",
    "desktop:prepare": "node scripts/neutralino/prepare-resources.js",
    "desktop:dev": "npm run desktop:prepare && neu run",
    "desktop:build": "npm run desktop:prepare && neu build --release",
    "desktop:build:embed": "npm run desktop:prepare && neu build --release --embed-resources"
  },
  "dependencies": {
    "@ffmpeg/core": "0.12.10",
    "@ffmpeg/ffmpeg": "0.12.15"
  },
  "devDependencies": {
    "@neutralinojs/neu": "11.7.2",
    "@sveltejs/vite-plugin-svelte": "7.1.2",
    "@tsconfig/svelte": "5.0.8",
    "svelte": "5.56.3",
    "svelte-check": "4.6.0",
    "typescript": "6.0.3",
    "vite": "8.0.16"
  }
}
